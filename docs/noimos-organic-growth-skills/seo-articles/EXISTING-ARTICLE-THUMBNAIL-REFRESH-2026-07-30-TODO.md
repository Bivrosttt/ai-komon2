# 既存記事ブラッシュアップ TODO（2026-07-30）

## 目的

更新済みのNoimos SEO/GEOスキルとサムネイル生成ツールを、既存28記事へ適用する。本文のURL、canonical、既存図解、リダイレクトは維持し、検索結果・SNS共有・AI引用の入口となる記事専用画像とメタデータを整える。

## 実行チェックリスト

- [x] 変更前ベースラインを保存（`content-refresh-baseline-before-thumbnail-refresh.json`）
- [x] 既存記事28件を対象にすることを確認（記事ハブは監査対象から除外）
- [x] slugごとの背景パレット・形状・白アイコンを決める
- [x] 記事ディレクトリへコード生成SVGサムネイルを作成する
- [x] 本文の最初のカバー画像も記事専用SVGへ統一する（古い図解はサムネイルとして表示しない）
- [x] 旧`hero-visual`を撤去し、サムネイルを公開日・著者行の直後へ配置する
- [x] `og:image` と `twitter:image` を記事専用サムネイルへ更新する
- [x] Article JSON-LDの `image` を同じサムネイルへ同期する
- [x] visible本文・H1・canonical・dateModified・CTAを維持し、既存の詳細図解ファイルは削除せず、冒頭カバー表示だけをSVGへ切り替える
- [x] 変更後ベースラインで薄記事・リンク問題が増えていないことを確認する
- [x] 28記事をAI-only SEOバリデーションする
- [x] 代表記事を1440px・390pxで表示し、横溢れ・画像ロード・目次・CTAを確認する
- [x] 参照Markdown、画像インベントリ、ハッシュを再生成する
- [x] 変更理由、対象指標、公開後28日レビュー日をレポートへ残す

## 成功指標

- 構造: 28/28のArticle JSON-LD・OG画像が記事専用サムネイルを指す
- 品質: 28/28のAI-only validatorが `pass`
- 表示: 代表記事で `overflow=false`、画像ロード完了、左目次・右CTAが表示される
- 効果: 公開後28日にGSC表示回数・クリック・CTR・平均順位、記事CTAクリックを比較

## ロールバック

この変更専用コミットを戻せば、HTMLメタデータと生成サムネイルを一括で復元できる。削除、リダイレクト、canonical変更は自動実行しない。
