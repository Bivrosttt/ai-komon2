<!-- 2026-07-22: 候補6本の操作パラダイム再設計仕様（着せ替え禁止） -->
# パラダイム再設計：GENBA / HANKYO / BRIEF / KITEI / SAIYO / SHAIN

禁止: 既存の「ログイン→左ナビ232px→KPI4枚→テーブル→詳細2ペイン」を色違いで再利用すること。
必須: デモフレーム帯・ログイン・hash・toast・CTA・analytics は残してよいが、**メインの情報設計と操作の主語を変える**。

参照してよいもの: 既存 works-*.html の JSユーティリティ（escapeHtml, toast, doLogin）だけ。UI骨格は新規。
完了済み: GENBA（マップ＋フィルム）/ HANKYO（SLA壁＋レーン）/ BRIEF（キャンバス）

---

## 4. KITEI — 条文が主語（リーダー＋付箋注釈）

ファイル: `works-kitei.html`（上書き）

### 操作の主語
**規程のページを読む**こと。検索結果リストがホーム、は禁止。

### 画面構成
- サイドバーなし。上部に Ask バー（サンプルチップ付き）
- メインは **見開きリーダー**: 左に目次（就業規則/稟議/出張）、右に条文本文（Serif・余白広め）
- 質問実行で該当条文へスクロール＋ **余白に付箋注釈**（出典ハイライト）が飛ぶ
- 下部シート or 右ドロワーで「回答まとめ」と「稟議下書き生成」

### AI演出
チップ選択 → 条文スキャン演出 → 付箋出現 → 回答カード → 稟議下書きタイピング

### ルック
藍グレー `#1E3A5F` / `#F1F5F9`、Noto Serif 条文。紙面感。

### サムネ
`assets/thumbs/works-kitei.jpg` をリーダー＋付箋構図で再生成

---

## 5. SAIYO — 流れが主語（採用カンバン）

ファイル: `works-saiyo.html`（上書き）

### 操作の主語
候補者カードを **レーンで進める**こと。候補者テーブルがホーム、は禁止。

### 画面構成
- サイドバーなし。上部に求人セレクト＋KPIチップ（少）
- メイン **4レーン看板**: `書類` | `一次面接` | `最終` | `内定`
- カードクリックで **右スライドインシート**: 面接メモ → AI評価表 → 合否メール
- 「進める」でレーン移動（HANKYOと同型だが採用文脈）

### AI演出
カード開く → 評価表生成（5軸）→ 通過/お見送りメール下書き → 送信でレーン進む

### ルック
紫 `#7C3AED` / `#F5F3FF`、M PLUS Rounded。丸いカード。

### サムネ
カンバンレーンが分かる構図

---

## 6. SHAIN — 記事が主語（Wiki＋Ask）

ファイル: `works-shain.html`（上書き）

### 操作の主語
**記事を開いて答えがハイライトされる**こと。CS受信箱スレッドがホーム、は禁止。

### 画面構成
- 上部巨大 **Askバー1本**（サンプル質問チップ）
- 左: ナレッジ記事リスト（Wiki）
- 右: 記事本文ビューア。Askすると該当段落がハイライト＋回答カードが記事上にオーバーレイ
- 「この回答をナレッジに追加」カードで完了

### AI演出
Ask → 記事が自動オープン → 段落ハイライト → 回答生成 → ナレッジ追加

### ルック
緑 `#059669` / `#ECFDF5`。ヘルプセンター／Notion寄り。

### サムネ
Askバー＋記事ハイライトが分かる構図

---

## 完了定義（各ファイル）
- 3秒で「別物」と分かる骨格
- AI演出1本以上
- モバイル最低限OK
- コミットはファイルごと日本語+emoji
- systems/works のリンク先は同じなので文言変更は任意
